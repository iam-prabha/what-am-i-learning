# Algorithms

Classic algorithms implemented in Python.

## 📁 Structure

This directory contains subdirectories for different algorithm categories. Currently, implementations are being added progressively.

### Sorting Algorithms (`sorting/`)
Planned implementations of common sorting techniques:
- Bubble Sort
- Selection Sort
- Insertion Sort
- Merge Sort
- Quick Sort
- Heap Sort

### Searching Algorithms (`searching/`)
Planned efficient search implementations:
- Linear Search
- Binary Search
- Jump Search
- Interpolation Search

### Graph Algorithms (`graph/`)
Planned graph traversal and path finding:
- Breadth-First Search (BFS)
- Depth-First Search (DFS)
- Dijkstra's Shortest Path
- Bellman-Ford Algorithm
- Minimum Spanning Tree (Kruskal, Prim)

## 🎯 Status

Algorithm implementations are being added progressively. Each subdirectory is ready for implementations.

### Adding New Algorithms

When implementing algorithms:
1. Create files in the appropriate subdirectory
2. Include clear comments and docstrings
3. Add time and space complexity analysis
4. Include example usage and test cases
5. Update this README with the new implementation

## 💡 Algorithm Complexity Reference

| Algorithm | Time Complexity (Best) | Time Complexity (Average) | Time Complexity (Worst) | Space Complexity |
|-----------|------------------------|---------------------------|-------------------------|------------------|
| Bubble Sort | O(n) | O(n²) | O(n²) | O(1) |
| Merge Sort | O(n log n) | O(n log n) | O(n log n) | O(n) |
| Quick Sort | O(n log n) | O(n log n) | O(n²) | O(log n) |
| Binary Search | O(1) | O(log n) | O(log n) | O(1) |
| Linear Search | O(1) | O(n) | O(n) | O(1) |
| BFS | O(V + E) | O(V + E) | O(V + E) | O(V) |
| DFS | O(V + E) | O(V + E) | O(V + E) | O(V) |

## 📚 Resources

- Data Structures: `../data_structures/`
- Python Fundamentals: `../../python/`
- Main DSA Guide: `../README.md`
